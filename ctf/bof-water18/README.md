# bof-water18 helper (optional)

This folder contains a reusable helper script with comments. It’s optional — the walkthrough uses a direct buffer overflow payload without scripting.

## What to edit
- `HOST` and `PORT` if the server changes.
- `OFFSET` if the buffer size changes.
- `TARGET` if the magic value changes.

## Usage
```
python3 exploit.py
```
